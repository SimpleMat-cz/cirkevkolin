import ListSermons from './ListSermons'
import CreateSermon from './CreateSermon'
import EditSermon from './EditSermon'

const Pages = {
    ListSermons: Object.assign(ListSermons, ListSermons),
    CreateSermon: Object.assign(CreateSermon, CreateSermon),
    EditSermon: Object.assign(EditSermon, EditSermon),
}

export default Pages